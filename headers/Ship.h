// // #include "SpaceObject.h"

// const float ANGULAR_STEER_SPEED = 5.0f;

// class Ship : public SpaceObject
// {
// public:
//     Ship(const float x, const float y, const float dx, const float dy, const int size, const float theta)
//         : SpaceObject(x, y, dx, dy, size, theta)
//         {
            
//         };
//     ~Ship();

//     void SteerLeft(float time);
//     void SteerRight(float time);
//     void Thrust(float acceleration, float time);

// private:
// };